import { motion } from "framer-motion";

function Index() {
  return (
    <motion.main className="flex w-full h-screen bg-background justify-center items-center">
    </motion.main>
  )
}

export default Index
